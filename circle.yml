machine:
    timezone:
        America/New_York
    python:
        version:
            3.5.0
    environment:
        USERNAME: yarnregressionuser

test:
    override:
        - nosetests -vv yarn/tests
